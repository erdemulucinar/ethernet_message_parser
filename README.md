# ethernet_message_parser
A simple translator from high level Ethernet message descriptions to vhdl code to parse these messages when triggered.

$/flex_bison folder contains the flex (emp.l) and bison (emp.y) files which are used for the front-end operations.

$/translator folder contains both front-end and back-end fuctionality.
